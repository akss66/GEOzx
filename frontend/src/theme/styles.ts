import type { CSSProperties } from "react";

export const silverTagStyle: CSSProperties = {
  background: "var(--dy-accent-wash)",
  borderColor: "var(--dy-border)",
  color: "var(--dy-text)",
};

export const neutralTagStyle = silverTagStyle;
