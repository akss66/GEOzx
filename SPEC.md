# Spec: 同舟行 AI 新媒体运营平台 v2

> 状态：Accepted for phased implementation
> 更新时间：2026-07-16
> 产品基线：`PRODUCT.md`
> 设计基线：`DESIGN.md`
> 执行清单：`tasks/current.md`
> 旧规格仅供历史参考，不再作为 UI 或产品结构约束。

## 1. Objective

将现有“模块后台 + 局部 Agent 功能”重构为面向代运营团队的 AI 原生运营平台，并保持已经完成的真实后端能力可用。

本阶段必须实现：

> 客户 / 项目 / 账号上下文 → 主 Agent 或独立专家 → 正式成果 → 风险审批 → 内容与发布准备 → 数据复盘 → 下一轮任务

每个重构模块必须前后端一起完成，正式环境不得使用 Mock 数据。

## 2. Users and Authorization

### 2.1 Roles

- 运营负责人
- 运营人员
- 编导 / 内容人员
- 审核员
- 账号管理员
- 系统管理员
- 技术管理员

### 2.2 Authorization model

权限由两部分叠加：

1. **资源范围**：团队、客户、项目。
2. **岗位角色**：负责人、运营、编导、审核、管理员等。

要求：

- 成员只能读取被分配的客户或项目。
- 同一成员可在不同项目具有不同岗位。
- API 必须校验资源范围，不能只依赖前端隐藏。
- 客户的账号、知识、成果、成本和 Agent 上下文不可跨客户泄露。
- 管理员默认不等于技术管理员；模型基础设施需要独立权限。

## 3. Business Hierarchy

### 3.1 Entities

- `Tenant / Org`：团队或未来 SaaS 租户。
- `Client`：代运营客户。
- `Project`：客户下的阶段性运营项目。
- `Account`：客户长期持有的平台账号资产。
- `ProjectAccount`：项目与账号的多对多关系。

### 3.2 Rules

- 一个客户可有多个项目和多个账号。
- 一个账号可参与多个项目。
- 任务、内容、成果、审批、指标和成本必须可追溯到客户和项目。
- 当前已有 `Org / Project / Account` 模型应通过兼容迁移扩展，不推翻真实 OAuth 和历史记录。

## 4. Global Application Behavior

### 4.1 Navigation

第一阶段保留当前一级导航名称：运营大脑、专家团、账号矩阵、内容生产、人工审批、运营复盘、使用成本、知识库。

管理员区拆为：用户管理、专家管理、模型基础设施。

### 4.2 Context switching

- 左上角切换客户和项目。
- 顶部显示当前平台和账号。
- 用户可以选择项目内的一个或多个账号。
- 主 Agent 可以建议切换，但必须由用户确认。
- 上下文切换后，页面数据和 Agent 会话必须刷新到新范围。

### 4.3 Global search

搜索范围：客户、项目、账号、任务、内容、成果、知识。

搜索结果可执行：

- 打开对象。
- 切换上下文。
- 创建任务。
- 进入审批。

结果必须经过权限过滤。

### 4.4 Global Agent

主 Agent 支持：

- 运营大脑主页面。
- 右下角全局入口。
- `Ctrl/Cmd + K`。
- 对象级“交给主 Agent”。

入口共享同一 Agent 会话和当前页面上下文。用户需明确选择“仅讨论”或“创建正式任务”。

### 4.5 Notifications

首版实现站内通知与统一待办中心：

- 审批请求。
- 任务完成或失败。
- 账号授权异常或过期。
- 数据同步失败。
- 项目风险。

预留企业微信、飞书、邮件和短信适配器，但首版不要求上线。

## 5. Agent Runtime

### 5.1 Main Agent behavior

- 普通问候、简单问答不得启动完整专家工作流。
- 正式运营目标由主 Agent分析后自主选择专家、工具和顺序。
- 不使用固定“每次调用全部专家”的机械流程。
- 缺失关键上下文时先询问用户。
- 高风险动作和项目审批规则触发人工确认。
- 运行时必须可暂停、恢复、打断、补充要求和重跑。

### 5.2 Expert isolation

每个专家具有：

- 独立 system prompt。
- 明确输入包。
- 工具白名单。
- 结构化输出契约。
- 当前客户 / 项目 / 账号范围。

专家不能跨客户读取知识、成果或账号数据。

### 5.3 Runtime ledger

继续复用并完善：

- `BrainTask`
- `AgentInvocation`
- `AgentToolCall`
- `Deliverable`
- `DeliverableAcceptance`
- `Event`

时间线事件至少包括：

- 用户消息。
- 主 Agent 回复。
- 专家开始 / 完成 / 失败。
- 工具请求 / 审批 / 结果。
- 暂停 / 恢复 / 打断。
- 正式成果生成。
- 任务完成 / 失败。

### 5.4 Presentation

- 默认视图只展示自然语言、专家接力、关键成果和审批。
- 完整详情展示工具参数、权限、耗时、成本、错误和重试。
- 所有结构化输出必须解析为业务 UI，禁止展示原始 JSON。
- 前端流式输出不能伪造后端未发生的专家调用。

### 5.5 Confirmation

人工确认通过底部对话输入器的确认模式完成，必须支持：

- 允许。
- 驳回。
- 输入修改意见并重跑。
- 首屏显示动作、对象、影响和风险，完整参数进入执行详情。
- 原位展开修改意见输入，不新增遮挡对话的大浮层。
- 查看动作对象、影响范围和风险。

## 6. Formal Artifacts

### 6.1 Adaptive result

- 简单答案留在对话中。
- 账号定位、策略、脚本、发布包和复盘生成正式成果文档。

### 6.2 Artifact actions

正式成果支持：

- 展开与收起。
- 编辑。
- 版本历史。
- 采用。
- 保存到项目成果库。
- 交给主 Agent。
- 进入审批或内容生产。
- 导出 / 复制。

### 6.3 Write protection

专家独立调用可以产出完整结果，但必须在用户点击“采用”后才写入账号定位、策略等正式业务字段。交给主 Agent 不等同于覆盖已有数据。

## 7. Module Requirements

### 7.1 运营大脑

#### Layout

- Claude / ChatGPT 式沉浸对话。
- 全局系统导航保留，但对话区占据最大可用宽度。
- 输入框始终可用并固定在底部。
- 不常驻右侧 Dashboard。

#### Start states

- 未选择客户：显示全局入口和少量最近任务。
- 未明确选择账号：顶部和运营大脑同时显示未选择，禁止隐式使用账号列表第一项，正式任务不可发送。
- 已选择客户 / 项目：显示简短晨报和输入入口。
- 存在运行中任务：恢复最近执行现场。

#### Motion

- 专家采用编辑部身份体系，例如职能代号和字标。
- 专家入场、并行、交接、审批暂停和成果揭示具备明显动画。
- 提供 reduced-motion 降级。

#### Acceptance

- “你好”等问候只产生主 Agent 回复。
- 运营目标能创建真实任务与 runtime events。
- 专家调用来自真实 `AgentInvocation`。
- 工具审批可暂停并恢复任务。
- 刷新后能恢复完整会话与运行状态。

### 7.2 专家团

#### User view

- 展示专家名称、编辑部代号、职责、能力、近期成果和调用记录。
- 支持进入专家独立对话。
- 独立调用携带当前客户、项目、平台和账号上下文。
- 结果可以采用、保存、继续修改或交给主 Agent。

#### Admin view

- 专家职责、Prompt、工具、权限、质量门和兜底策略。
- 模型供应商和密钥不在普通专家配置中直接展示。

### 7.3 账号矩阵

#### Views

- 表格视图（默认）。
- 矩阵视图。
- 卡片视图。
- 项目视图。

#### Required fields

- 头像。
- 平台。
- 昵称与外部 ID。
- 账号定位。
- 客户与项目。
- 授权状态。
- 数据状态。
- 发布能力。
- 当前任务。
- 风险和最近同步时间。

账号列表中的风险数与“当前任务”使用同一口径，只显示最新活动任务的风险；历史待验收任务进入人工审批队列，不得在账号卡上机械累加。发布能力必须区分“可准备发布包”“人工发布”和“暂不可用”，不得用授权状态代替发布权限。

#### Operations

- 接入账号。
- 重新授权。
- 同步数据。
- 分组与绑定项目。
- 批量选择和批量操作。
- 记住个人视图与筛选偏好。

账号矩阵读取必须同时经过客户 / 项目资源范围过滤。审核员可查看被分配账号但不能执行分发；负责人和运营可记录分发动作；平台应用配置、OAuth 授权和数据同步由系统管理员执行。前端隐藏不能替代 API 权限校验，越权资源统一按不存在处理。

状态必须来自真实 OAuth、Token、权限和同步结果。账号管理接口不得把抖音账号手工写成
`connected / authorized / healthy`；这些官方状态只能由 OAuth 回调、Token 生命周期和真实同步任务更新。
本地开发账号必须使用独立的 `manual / manual / manual` 状态，并在界面中明确区别于官方授权账号。
已经进入官方接入流程的账号不得被账号管理接口降级或覆盖成开发模式。

### 7.4 内容生产

采用自适应工作区：

- 默认内容画布。
- 素材面板。
- 版本面板。
- 阶段视图。
- Agent 协作面板。
- 审批与发布包入口。

面板按需展开，不得同时占据正文空间。支持选题、脚本、分镜、素材、成片和发布包的类型化编辑器。

### 7.5 人工审批

采用三段式：

- 左侧队列和筛选。
- 中间完整预览。
- 右侧风险、Agent 解释、影响和操作。

审批类型：

- 定位、选题、脚本和成片（按项目规则）。
- 发布包。
- 外部平台动作。
- 账号授权、删除和其他高风险工具。

默认强制审批：发布、删除、账号授权、投流。投流当前不实现执行能力。

### 7.6 运营复盘

顺序固定为：

1. 目标完成度。
2. 关键变化。
3. 内容和动作归因。
4. 数据证据。
5. Agent 建议。
6. 创建下一轮任务。

必须标明数据来源、同步时间和缺失原因。没有指标时不能渲染假图表。

### 7.7 使用成本

运营负责人视图：

- 客户 / 项目预算。
- 任务与专家成本。
- 趋势和预算告警。

技术管理员视图：

- 模型、Token、供应商。
- 工具和素材生成成本。
- 失败与重试成本。

### 7.8 知识库

- 手工创建与编辑。
- Agent 建议沉淀。
- 用户确认后写入。
- 来源、版本、范围、引用和使用记录可追踪。
- Agent 输出显示使用过的知识来源。

### 7.9 用户与管理中心

- 用户管理必须支持创建、编辑、停用和资源授权。
- 专家管理与模型基础设施分离。
- 专家管理必须支持专家启停、中文职责、组织补充指令、工具权限和质量门配置。
- 专家停用后不得被独立调用或被主 Agent 新计划选中；若目标没有可用专家，系统必须明确阻断并说明原因。
- 工具权限必须真实写入运营大脑计划和 `AgentToolCall`：自动执行不暂停，确认或人工模式进入权限门，禁用工具不得生成调用。
- 专家业务配置变更必须写入不可变审计事件；普通成员无权读取或修改管理配置。
- 专家管理响应不得包含供应商密钥、模型参数或调用日志。
- 技术管理员可以配置供应商、模型、参数、密钥引用、兜底和调用日志。
- Secret 永远不返回前端明文。
- 模型基础设施接口仅管理员可访问；普通成员访问供应商、路由和调用账本必须返回 `403`。
- 供应商配置只接受服务端认可的 `env:` 或 `vault://` 引用，不接受明文 Key、任意环境变量名或任意服务端点。
- 每个专家路由独立保存首选模型、可选兜底、温度、最大输出 Token 和超时时间；业务专家配置必须与路由参数相互保留，不得互相覆盖。
- 供应商启停与路由参数必须进入真实 LLM Gateway；供应商停用或凭证未配置时调用失败要写入不可变账本，并按已配置兜底继续。
- 调用账本记录专家、供应商、模型、Token、成本、延迟、状态和脱敏错误摘要；历史记录不得由管理页修改。
- 供应商和路由变更写入不可变审计事件，响应和审计负载均不得包含 Secret 明文。

## 8. Platform Integrations

### 8.1 Douyin

抖音继续采用官方 OAuth / OpenAPI：

- Client Secret 只存服务器或安全密钥引用。
- OAuth token 加密存储。
- 使用真实授权状态、Token 过期时间和数据同步结果。
- 当前新应用审核期间允许测试白名单和测试账号授权。
- 正式发布与作品数据能力必须等待相应开放平台权限。

### 8.2 Other platforms

小红书和视频号首版只保留平台适配器、能力矩阵和明确占位状态，不得显示为已接入。

### 8.3 Prohibited production paths

- Cookie 文件作为抖音主授权方式。
- 浏览器自动点击创作者中心作为生产主链路。
- 绕过平台限制的自动化。
- 未经确认的自动发布。

## 9. Data Truth and States

### 9.1 No production mock

正式环境禁止：

- 假图表。
- 假任务。
- 假专家调用。
- 假成本。
- 假授权与假同步状态。

### 9.2 Empty state

- 保留真实页面结构。
- 说明缺失数据或前置条件。
- 只提供一个主要行动。
- 可选“交给主 Agent”。

### 9.3 Error state

- 显示可理解的错误原因。
- 提供重试。
- 管理员可进入诊断详情。
- 禁止无限转圈。

## 10. API Evolution

优先保持现有 API 兼容，按模块增量扩展：

- `/auth/*`
- `/users/*`
- `/brain/*`
- `/agents/*`
- `/accounts/*`
- `/platform-integrations/*`
- `/content-items/*` 及现有 orchestrator 等价接口
- `/approvals/*` 或现有 gate / tool-call 审批接口
- `/metrics/*`
- `/costs/*`
- `/knowledge/*`
- `/materials/*`

新增客户、项目账号多对多、通知、成果库和偏好设置时，应提供迁移脚本和旧数据兼容策略。

## 11. Responsive Requirements

移动端首版必须完成：

- 主 Agent 对话和运行状态。
- 审批与修改意见。
- 通知和待办。
- 客户 / 项目 / 平台 / 账号切换。
- 账号授权与同步状态。

复杂编辑、批量矩阵和技术配置可以提示使用桌面端，但页面不得溢出或不可操作。

## 12. Delivery and Acceptance

每个模块必须经过：

1. 高保真视觉稿确认。
2. 前端和后端同步实现。
3. 单元 / 接口测试。
4. localhost 桌面和移动端验收。
5. 用户明确批准。
6. 生产部署与冒烟测试。

模块完成标准：

- 使用真实接口和真实状态。
- 关键操作可完成。
- 权限和客户隔离有效。
- 加载、空、失败和边界状态完整。
- 无乱码、原始 JSON、无限加载和明显布局溢出。
- 未经用户批准不部署线上。

## 13. Implementation Order

1. 设计系统与 App Shell。
2. 运营大脑。
3. 账号矩阵。
4. 内容生产。
5. 人工审批。
6. 运营复盘。
7. 专家团。
8. 知识库。
9. 使用成本。
10. 用户管理、专家管理和模型基础设施。
11. 移动端关键操作收口。

## 14. Out of Scope

- 客户门户。
- 新手引导。
- 深色主题。
- 投流自动化。
- 非官方抖音发布自动化。
- 小红书 / 视频号真实 OAuth。
- 完整 SaaS 计费。
- 跨客户业务数据比较。
