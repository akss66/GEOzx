# Spec: 同舟行 AI 新媒体运营平台 v1

> 状态：Accepted for v1
> 更新时间：2026-07-06
> 旧版完整规格已归档：`docs/archive/2026-07-doc-reset/SPEC.before-v1-reset.md`
> 当前执行清单：`tasks/current.md`

## 1. 产品定义

同舟行 AI 新媒体运营平台是一个以 **AI + Agent + 运营** 为核心的新媒体运营工作台。

它不是传统运营后台，也不是单一聊天机器人。用户先选择当前平台和账号，再把运营目标交给运营大脑；运营大脑负责生成结构化 Brief、调度专家 Agent、组织交付物、进入人工审批，并把复盘结果回流成下一轮运营任务。

v1 只服务一个清晰目标：

> 让一个运营团队围绕单个或多个抖音账号，完成“目标输入 -> 专家编排 -> 内容/发布准备 -> 人工审批 -> 复盘回流”的可追踪闭环。

DyFlow 仅作为内部代号或技术模块名保留，不作为前端主品牌。

## 2. 当前用户

- **管理员**：管理用户、Agent 配置、平台接入配置和系统安全边界。
- **运营负责人**：选择平台账号，输入运营目标，确认 Brief，监督专家 Agent 执行。
- **内容运营 / 编导 / 视觉 / 剪辑**：查看专家产出，补充素材，验收或打回交付物。
- **审核员**：处理发布前确认、风险项、质量门和人工审批。
- **账号矩阵管理员**：维护抖音账号授权状态、数据同步状态、发布能力状态。

## 3. v1 模块边界

左侧主导航保留：

1. 运营大脑
2. 专家团
3. 账号矩阵
4. 内容生产
5. 人工审批
6. 运营复盘
7. 使用成本
8. 知识库

管理员额外可见：

1. 用户管理
2. Agent 配置

当前阶段不把投流、客服、自动发布作为主导航或验收主线。它们可以保留模型、枚举、适配器或后续入口，但 UI 不能暗示当前已经可用。

## 4. 平台接入策略

### 4.1 当前平台

- **抖音**：v1 优先平台，走官方 OAuth / OpenAPI 路线。
- **小红书**：v1 只保留占位能力、手动回填状态和未来适配器接口。
- **视频号**：v1 只保留占位能力、手动回填状态和未来适配器接口。

### 4.2 抖音接入原则

- 账号授权必须基于抖音开放平台官方 OAuth。
- Client Secret 不允许写入前端源码。
- 前端可填写或修改允许前端配置的 Client Key、回调地址、安全域名、权限范围等配置。
- OAuth token 必须真实安全存储，或明确显示为未配置；不能只靠默认 secret 引用或手动标记显示“已授权”。
- 生产回调必须使用公网域名，不能依赖临时隧道作为长期方案。

### 4.3 暂不采用

- 不把 Cookie 登录文件管理作为抖音主链路。
- 不使用 Playwright / Patchright 自动点击创作者中心作为当前主链路。
- 不复制绕过平台限制、反爬签名、非官方发布的实现。
- 不在审核通过前做抖音自动发布。

## 5. 核心体验

### 5.1 全局账号上下文

系统顶部栏必须显示当前工作上下文：

- 当前平台：v1 默认抖音。
- 当前账号：昵称、平台、授权状态、数据同步状态。
- 未选账号时显示明确的“选择抖音账号”状态。

运营大脑、内容生产、人工审批、运营复盘都继承当前账号上下文。没有当前账号时，不允许创建需要账号绑定的运营任务。

### 5.2 运营大脑

运营大脑是登录后的默认首页。

首屏结构：

- 当前平台 / 当前账号状态。
- 主 Agent 大输入框。
- 结构化 Brief 预览。
- 专家 Agent 编排流。
- 当前任务、风险、审批和复盘入口。

用户输入目标后，系统流程为：

1. 主 Agent 接收目标。
2. 主 Agent 生成结构化 Brief。
3. 用户确认 Brief。
4. 主 Agent 调用一个或多个专家 Agent。
5. 专家 Agent 以专业身份卡片呈现，状态包括待启动、处理中、已完成、需确认、已阻塞。
6. 每个专家完成后默认展示一句核心结论，可展开详情。
7. 高风险动作进入人工审批。
8. 任务结果进入复盘链路。

### 5.3 专家团

专家团不是“8 个聊天窗口”，而是运营大脑可调度的专业能力池。

v1 专家结构：

- 运营大脑 / 决策 Agent
- 账号定位专家
- 内容策略专家
- 编导文案专家
- 美术提示词专家
- 视频创作专家
- 剪辑专家
- 账号运营专家
- 发布准备专家

专家团页面展示：

- 专家身份和职责。
- 当前任务。
- 最近调用。
- 工具权限。
- 模型配置。
- 成本和失败记录。

直接调用专家只作为管理员或调试入口，普通运营主流程应从运营大脑发起。

### 5.4 内容生产与发布准备

内容生产页不只做内容检查，而要生成平台可执行的发布包。

发布包字段：

- `platform`
- `account_id`
- `content_type`
- `title`
- `body`
- `topics`
- `material_ids`
- `cover_material_id`
- `scheduled_at`
- `visibility`
- `allow_comment`
- `execution_mode`
- `manual_steps`

发布准备专家工具：

- 工具名：`publish_package_prepare`
- 职责：校验素材、标题、正文、话题、封面、定时发布时间和发布模式。
- 输出：发布包、阻断项、人工检查清单。
- 记录：必须写入 `AgentToolCall` 或现有等价工具调用账本。

当前抖音正式发布权限通过前，发布包审批通过后只进入 `approved_for_manual_publish`，不自动发出。

### 5.5 人工审批

人工审批页承接所有高风险动作：

- 发布包确认。
- 质量门阻塞。
- 敏感内容。
- 账号授权异常。
- 需要人工介入的工具调用。

审批页必须展示：

- 当前平台和账号。
- 内容类型。
- 标题、正文、话题。
- 素材和封面。
- 定时发布。
- 可见范围。
- 评论设置。
- 检查结果。
- 一键复制发布清单。

审批状态至少包括：

- `pending`
- `approved`
- `rejected`
- `approved_for_manual_publish`
- `blocked`

### 5.6 账号矩阵

账号矩阵是平台账号的事实来源。

v1 必须展示：

- 平台。
- 账号昵称。
- 外部账号 ID。
- 授权状态。
- 数据同步状态。
- 发布能力状态。
- 最近同步时间。
- 异常原因。

状态表达必须真实：

- “已配置”不能只因为存在默认 secret 引用。
- “已授权”不能只因为用户手动标记。
- “token_configured” 必须对应真实可用 token，或明确标记为待配置。

### 5.7 运营复盘

运营复盘必须按当前账号读取数据。

v1 复盘字段：

- 任务。
- 发布包。
- 审批记录。
- 工具调用记录。
- 作品 ID。
- 发布时间。
- 播放、点赞、评论、收藏、转发。
- 完播率或平台可提供的等价指标。
- Agent 建议。

抖音数据回流优先走官方 OpenAPI。审核或权限不足时，可以使用手动回填模型，但 UI 必须明确标注来源。

### 5.8 使用成本

使用成本统计：

- Agent 调用次数。
- 工具调用次数。
- 模型 token。
- 模型费用。
- 素材生成费用。
- 失败重试成本。

成本必须能按任务、账号、专家 Agent 维度查看。

### 5.9 知识库

知识库用于沉淀：

- 账号定位。
- 爆款案例。
- 标题模板。
- 话题模板。
- 脚本片段。
- 封面要求。
- 平台规则。
- 复盘结论。

知识库内容可作为主 Agent 和专家 Agent 的上下文来源，但必须保留可追踪引用。

## 6. 后端事实模型

v1 主要领域模型：

- `User` / `Role`：用户和权限。
- `Workspace` / `Project`：组织与项目。
- `Account`：内部账号对象。
- `PlatformIntegration`：平台接入配置。
- `PlatformAccountAuth`：平台账号授权状态。
- `BrainTask`：运营大脑任务。
- `TaskBrief`：结构化 Brief。
- `OrchestrationPlan`：专家调度计划。
- `AgentInvocation`：专家调用记录。
- `AgentToolCall`：工具调用账本。
- `Deliverable`：交付物。
- `DeliverableAcceptance`：分项验收。
- `PublishPackage` 或等价 JSON 结构：发布包。
- `PerformanceSnapshot`：复盘指标快照。
- `CostRecord`：成本记录。

原则：

- 运营大脑任务必须绑定 `platform + account_id`。
- 专家调用和工具调用必须可追踪。
- 高风险动作必须可审批。
- UI 状态必须从真实后端字段推导，不能从 mock 或默认值冒充。

## 7. API 边界

v1 API 分组：

- `/auth/*`：登录、当前用户、权限。
- `/brain/*`：运营大脑任务、Brief、确认、调度、验收、重跑。
- `/agents/*`：专家列表、详情、配置、管理员调试调用。
- `/platform-integrations/*`：平台配置、授权链接、OAuth 回调、JS SDK 签名、账号授权状态。
- `/accounts/*`：账号矩阵基础管理。
- `/materials/*`：素材管理。
- `/publishing/*` 或等价分组：发布能力矩阵、发布包准备、发布清单。
- `/approvals/*` 或等价分组：人工审批。
- `/metrics/*`：平台指标和复盘快照。
- `/costs/*`：模型和工具成本。
- `/knowledge/*`：知识库。

所有会改变外部平台状态或进入发布链路的 API，都必须经过权限检查、参数校验和审计记录。

## 8. 前端设计要求

设计系统以 `DESIGN.md` 为准。

v1 关键要求：

- 默认浅色。
- ChatGPT 风格字体栈。
- 黑白灰极简。
- iOS 风格圆角，但不能过度圆润。
- 红色 logo 只作为品牌识别，不做大面积主题色。
- 页面要像 AI Agent 运营工作台，不像传统蓝白后台。
- 状态文字不能过浅，不能只靠颜色表达。
- 每个模块都要明确当前数据是真实、待接入、手动回填还是演示。

## 9. 当前非目标

v1 不做：

- 千川/投流自动化。
- 大额预算自动投放。
- 客服自动回复。
- 评论区自动私信或导流。
- 抖音创作者中心浏览器自动发布。
- 小红书 / 视频号真实 OAuth。
- 多租户 SaaS 商业化。
- 长期记忆。
- 未经人工确认的自动发布。

## 10. v1 验收标准

系统达到 v1 可用，必须满足：

- 用户可以登录系统。
- 管理员可以管理用户和 Agent 配置。
- 顶部栏可以显示或选择当前抖音账号。
- 未选择账号时，运营大脑不能创建账号绑定任务。
- 用户可以在运营大脑输入目标并生成结构化 Brief。
- 用户确认 Brief 后，系统生成专家调度计划。
- 专家调用记录、工具调用记录、状态、失败原因可追踪。
- 内容生产可以形成发布包。
- 发布包可以进入人工审批。
- 审批通过后，在抖音发布权限未通过前只进入人工发布状态。
- 账号矩阵不虚假显示“已授权/已配置”。
- 运营复盘可以按当前账号展示任务、发布包、工具调用和指标来源。
- 使用成本可以按任务、账号、专家 Agent 查看。
- 前端构建通过。
- 后端关键 API 有测试或可复现验证。

## 11. 当前优先级

当前执行顺序以 `tasks/current.md` 为准：

1. 修正事实源和安全边界。
2. 重构运营大脑模块。
3. 落地发布准备与人工审批。
4. 修正账号矩阵和平台状态真实性。
5. 打通运营复盘和成本闭环。

每完成一个模块，先本地验收，再部署到公网服务器。
