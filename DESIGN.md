---
name: 同舟行 AI 新媒体运营平台
description: 以登录页为品牌原点，扩展为暖纸框架、柔白画布、墨黑排版与克制品牌红组成的 AI 原生运营设计系统
version: 2.0
status: accepted-design-baseline
colors:
  brand_red: "#C9161D"
  brand_red_hover: "#AE1218"
  warm_canvas: "#EEE8DF"
  warm_canvas_deep: "#E4DCD0"
  work_surface: "#F7F7F4"
  surface_white: "#FFFFFF"
  surface_muted: "#F0F0ED"
  ink: "#171614"
  ink_soft: "#4A4640"
  ink_muted: "#666159"
  line: "#DDD8CF"
  line_subtle: "#EBE7DF"
  success: "#2B8152"
  warning: "#9A6300"
  error: "#B92B36"
  info: "#526774"
typography:
  family: '"Geist Variable", "Noto Sans SC Variable", "PingFang SC", "Microsoft YaHei UI", "Segoe UI", sans-serif'
  body: "14px / 1.62 / 400"
  ui: "13px / 1.4 / 500"
  title: "24px / 1.25 / 650"
  display: "40px / 1.08 / 700"
radius:
  control: "8px"
  object: "10px"
  floating: "12px"
---

# Design System: 同舟行 2.0

> 2026-07-17 起，系统 UI 唯一高保真基准为
> `docs/superpowers/specs/2026-07-17-high-fidelity-system-ui-design.md`。
> 旧模块局部样式只能补充业务布局，不得覆盖该基准的色彩、壳层、圆角和层级。

## 1. Design North Star

**在一张有温度的纸面上，运行一支具有专业身份和执行力的 AI 运营团队。**

当前登录页是品牌原点，但不是所有页面的直接模板。系统从登录页提取以下基因：

- 暖象牙纸面，而非冷白 SaaS 灰。
- 柔白工作画布，用于长时间阅读、编辑和数据操作。
- 墨黑强排版，建立清晰而前卫的品牌性格。
- 克制品牌红，只用于品牌、焦点、关键动作和 Agent 身份。
- 柔和空间层次，不依赖大阴影和卡片墙。

设计目标不是让所有页面长得相同，而是让不同工作模式属于同一个产品：

- 运营大脑像 Claude / ChatGPT 式对话空间。
- 账号、审批、用户和配置像 Linear 式高效工具。
- 内容与知识像 Notion 式可编辑工作区。
- 复盘像一份数据驱动的运营叙事报告。

## 2. Visual Language

### 2.1 Color Roles

#### Brand frame

- `Warm Canvas #EEE8DF`：应用外围、导航、品牌区域和低密度背景。
- `Warm Canvas Deep #E4DCD0`：选中区域、分组和次级背景。
- `Brand Red #C9161D`：Logo、主焦点、关键操作、专家字标和关键进度。

#### Work surfaces

- `Work Surface #F7F7F4`：主要内容画布。
- `Surface White #FFFFFF`：编辑器、表格、浮层和需要最高对比度的区域。
- `Surface Muted #F0F0ED`：输入区、次级分组、只读内容。

#### Ink and lines

- `Ink #171614`：标题、正文、主按钮和关键状态。
- `Ink Soft #4A4640`：次级正文。
- `Ink Muted #666159`：元信息；必须满足可读性，禁止过浅灰。
- `Line #DDD8CF`：对象边界。
- `Line Subtle #EBE7DF`：表格行、文档区块和轻分隔。

#### Semantic color

成功、警告、错误和信息色只表达状态，不参与主题装饰。所有状态必须同时有文字或图标，不能只靠颜色。

### 2.2 Material

- 暖纸质感只存在于底层背景，纹理强度不得影响文字和图表。
- 工作区保持清晰，不把内容容器全部做成毛玻璃。
- 禁止渐变文字、紫蓝科技渐变、发光球、光斑和装饰性噪点。
- 阴影只用于浮层、输入悬浮区和需要脱离画布的对象。

### 2.3 Typography

- 字体随前端打包：拉丁字符、数字和 UI 符号使用 `Geist Variable`，中文使用 `Noto Sans SC Variable`，避免不同电脑回退到不同系统字体。
- 正文使用 400，操作和标签使用 520/620，标题使用 700；不使用伪造粗体或过度细碎的字重。
- 中文正文优先保证 `PingFang SC` / `Microsoft YaHei` 可用。
- 不使用随视口缩放的字号。
- 字间距固定为 `0`。
- 页面标题保持克制；大字号只用于运营大脑起始状态、成果揭示和关键叙事结论。
- 表格、配置和紧凑面板使用更小、更稳定的 UI 字号。

### 2.4 Shape

采用 iOS 式连续圆角观感，但按对象控制：

- 输入、按钮、选择器：8-12px。
- 独立账号、成果、审批对象：12-16px。
- 浮层、全局 Agent 面板：16-20px。
- 表格、页面大区块默认不做浮动圆角卡片。
- 禁止卡片嵌套卡片。

## 3. Global App Shell

### 3.1 Navigation

第一阶段保留当前一级导航名称与顺序，使用**全局系统导航**：

1. 运营大脑
2. 专家团
3. 账号矩阵
4. 内容生产
5. 人工审批
6. 运营复盘
7. 使用成本
8. 知识库

管理员区包含用户管理、专家管理和模型基础设施。

导航规则：

- 左侧导航不使用巨大灰色胶囊选中态。
- 当前项通过墨黑文字、品牌红标记和轻背景共同表达。
- 管理员入口与日常运营入口明确分区。
- 导航可在移动端收起，但桌面端不得抢占主画布。

### 3.2 Context

- 左上角负责切换客户和项目。
- 顶部紧凑显示当前平台与账号。
- 上下文可点击切换，但不在每页堆放四个常驻大下拉框。
- 主 Agent 可建议切换上下文，用户确认后执行。
- 页面和 API 都必须以当前客户、项目和账号为明确范围。

### 3.3 Search and Agent

- 全局搜索负责定位客户、项目、账号、任务、内容、成果和知识，并可执行准确命令。
- 主 Agent 负责理解复杂需求、讨论和任务编排。
- 主 Agent 支持右下角入口、`Ctrl/Cmd + K` 和对象级“交给主 Agent”。
- 三种入口共享同一个上下文会话。

## 4. Density and Layout

采用模块自适应密度：

- 运营大脑、内容创作：宽松沉浸。
- 账号矩阵、审批、用户管理、专家管理、模型基础设施：紧凑高效。
- 运营复盘：结论宽松，证据和明细紧凑。

页面结构主要依靠：

1. 排版层级。
2. 稳定网格。
3. 留白和分隔线。
4. 对象级容器。

卡片只用于账号、成果、审批项、通知等真正独立对象。禁止把页面章节全部做成浮动卡片。

## 5. Module Design

### 5.1 运营大脑

采用 Claude / ChatGPT 式沉浸对话：

- 对话占据页面绝对中心。
- 输入框固定在底部并保持可用。
- 侧边导航只承担系统模块，不常驻风险卡、任务卡和专家列表。
- 客户、项目、平台和账号以紧凑上下文显示。

### Identity and model governance

- 成员管理采用桌面双栏治理工作台，右侧分为概览、资源权限、安全与登录、操作记录。
- 成员资源授权按客户、项目和账号三级表达，并持续显示最终生效权限。
- 禁用是可恢复操作；永久删除必须先展示影响预览，再验证成员邮箱和管理员独立二级密码。
- 管理员二级密码不可回显，设置或重置后有 10 分钟冷却期。
- 模型基础设施使用组织级供应商注册表，支持预置供应商和自定义 OpenAI 兼容供应商。
- API Key 只允许写入后端加密存储，前端与读取接口永远不接收完整密钥。
- 未验证成功的供应商不能成为新的主路由；被路由引用的供应商不能直接删除。
- 顶部账号切换器与对话运行时必须共享同一账号 ID；未明确选择时保持空状态，禁止默认取第一个账号。
- 普通问候直接自然回复，不启动专家流程。
- 正式目标由主 Agent 自主编排专家和工具。

Agent 时间线默认只展示：

- 主 Agent 的自然语言。
- 专家开始、完成和关键结论。
- 正式成果。
- 人工确认请求。
- 失败或阻塞的业务说明。

完整执行详情折叠展示工具参数、权限、耗时、成本、错误和重试。普通用户永远不看原始 JSON。

#### Expert identity

专家采用“编辑部身份体系”：

- 使用职能代号与专属字标，例如 `PX / POSITION`。
- 展示中文职责、当前任务和接力编号。
- 不使用虚构真人头像或卡通员工。
- 专家入场、并行、交接、暂停和完成具有明显空间与节奏变化。

#### Decision and permission interaction

- 方案选择是对话内容：使用 2-4 个结构化选项，显示一句说明、收益、代价和主 Agent 建议；用户也可要求重给方案或输入自己的方向。
- 权限确认不是独立弹窗：底部对话输入器原位切换为紧凑确认模式，首屏只显示动作、对象、影响和风险。
- 确认模式提供“驳回 / 修改要求 / 允许”；修改要求在同一输入器内展开，完成后恢复普通输入模式。
- 方案选择不等于权限授权；后续高风险动作仍须单独确认。
- 完整工具参数、耗时、成本与审计信息只进入执行详情。

#### Motion

运营大脑允许强表现力：

- 专家字标从对话层级中清晰浮现。
- 并行专家形成可理解的空间编排。
- 人工确认时页面节奏暂停，底部输入器平滑切换为确认模式，不新增遮挡对话的大浮层。
- 成果完成时从专家输出过渡为正式成果文档。
- 不使用流程图、粒子、霓虹、持续脉冲或无意义装饰运动。
- 必须提供 `prefers-reduced-motion` 降级方案。

### 5.2 专家团

- 普通视图展示专家能力、正式成果和工作记录。
- 专家可独立进入对话并产出正式结果。
- 结果动作包括：直接采用、交给主 Agent、继续修改、保存成果、导出。
- 采用或写入正式数据前必须确认。
- 管理配置不与普通专家主页混排。

### 5.3 账号矩阵

采用 Linear 式高效工作台：

- 默认高密度表格。
- 支持表格、矩阵、卡片、项目视图切换。
- 固定展示头像、平台、定位、状态、授权、数据、任务和风险。
- 支持批量选择与批量操作。
- 筛选器使用紧凑工具栏，不占据整行大卡片。
- 平台配置与账号列表分层，不重复展示相同状态。

### 5.4 内容生产

采用自适应工作区：

- 默认是无干扰内容画布。
- 左侧或顶部提供紧凑工具入口。
- 素材、版本、阶段、审批和 Agent 面板按需展开。
- 展开侧栏不得挤压正文到不可读宽度。
- 文档、脚本、分镜和发布包使用相应的结构化编辑器，而不是一张万能表单。

### 5.5 人工审批

采用三段式审批台：

- 左：待审批队列与筛选。
- 中：完整预览，使用最大可用空间。
- 右：风险、Agent 解释、影响范围、修改意见和审批动作。
- 确认面板允许直接选择，也允许输入意见后“驳回并重跑”。
- 审批完成后自动定位下一项。

### 5.6 运营复盘

采用数据叙事：

1. 先展示目标完成度和一句核心结论。
2. 展示关键变化和异常。
3. 用图表与作品证据解释原因。
4. 展示 Agent 建议和置信依据。
5. 提供“创建下一轮任务”。

禁止一进入页面就展示大面积空图表容器。

### 5.7 知识库

- 采用 Notion 式文档与集合体验。
- 清晰展示来源、版本、适用范围和引用记录。
- Agent 的“建议沉淀”作为待确认对象，不自动写入。

### 5.8 Cost and Admin

- 成本页按角色隐藏技术细节。
- 专家管理面向业务管理员。
- 模型基础设施面向技术管理员。
- 模型名、Token、Prompt 和供应商日志不出现在普通业务页面。
- 专家管理采用桌面双栏治理工作台：左侧为专家目录与启停状态，右侧编辑中文职责、组织补充指令、工具权限和质量门。
- 工具权限必须使用明确的 `自动执行 / 每次确认 / 仅人工执行 / 禁用` 业务语义，不展示内部枚举。
- 质量门区分组织可选规则和系统强制规则；强制规则不可关闭，并解释其审批影响。
- 保存和恢复操作固定在工作区底部，切换专家时不得静默丢弃未保存变更。
- 模型基础设施采用独立桌面技术工作台，不与业务专家配置混排；顶部只展示就绪供应商、专家路由、24 小时调用和失败数。
- 供应商、路由策略、调用账本使用三个明确视图：供应商配置仅接受服务器密钥引用；路由按专家编辑首选、兜底和运行参数；调用账本只读且错误信息脱敏。
- 技术配置使用高密度目录与编辑器布局，不使用 Dashboard 卡片墙；供应商状态、路由来源和失败结果必须可扫描、可定位、可审计。

## 6. Components

### Buttons

- 主操作：墨黑底白字；关键 Agent 确认可使用品牌红。
- 次操作：透明或柔白底、清晰边线。
- 危险操作：红色文字或红色确认，不将所有红色按钮都视为危险。
- 图标按钮优先 Lucide 图标，必须有 tooltip 和可访问名称。

### Inputs

- 对话输入框是运营大脑的主要交互对象，可使用更明显的浮层和圆角。
- 表格筛选与配置输入保持紧凑。
- placeholder 不预填真实值，不覆盖文本光标。
- 校验错误仅在提交或失焦后出现；输入修正后立即消失。

### Tables

- 稳定行高、固定列宽策略和响应式最小宽度。
- 表头不使用厚重灰底。
- 行操作优先使用图标或菜单，避免塞满文字按钮。
- 空表格保留表头和主要行动，不显示巨大插画。

### Formal artifacts

- 简单答案直接显示。
- 定位、策略、脚本、发布包和复盘生成正式成果文档。
- 成果必须支持版本、编辑、采用、审批、保存、导出和交给主 Agent。

### Conversation decisions

- 方案选择直接位于主 Agent 对话之后，不跳转、不弹窗。
- 默认展示 2-4 个互斥选项，并清晰标记建议方案、收益和代价。
- 用户可选择、要求再给一组方案，或输入自己的方向。
- 选择完成后保留在时间线中，并转为只读决定记录。

### Composer confirmation mode

- 权限请求到来时，底部输入器原位变为紧凑确认条，不在其上方堆叠大面板。
- 首屏显示动作、对象、影响和风险；完整参数进入执行详情。
- 提供允许、驳回和修改要求；修改要求在当前输入器内展开并触发重跑。
- 高风险动作明确标注对象、范围和不可逆后果。

### Empty, loading and error

- 保留真实页面骨架。
- 只显示一个主要行动，并可选“交给主 Agent”。
- 禁止巨大空白卡片、通用插画和永久转圈。
- 加载失败显示原因、重试和诊断入口。

## 7. Responsive Design

移动端首版支持：

- 主 Agent 对话与任务跟进。
- 审批和修改意见。
- 通知、待办与风险。
- 客户、项目、平台、账号切换。
- 账号授权和数据状态查看。

复杂内容编辑、批量矩阵管理、配置和深度复盘以桌面端为主。移动端三栏页面改为队列 → 预览 → 操作的分步导航，不能简单压缩成窄三栏。

## 8. Theme Strategy

- 首版只完成浅色主题。
- 暂时移除无实际作用的月亮切换按钮。
- 深色主题后续独立设计，不机械反转颜色。

## 9. Quality Bar

### Do

- 使用真实数据、真实状态和明确行动。
- 让每个模块拥有适合其任务的布局。
- 让 Agent 的工作可见但不把技术日志倾倒给用户。
- 在运营大脑中使用强而专业的动态表达。
- 使用稳定尺寸与响应式约束避免跳动、溢出和遮挡。

### Do not

- 不做传统 Dashboard 卡片墙。
- 不做所有页面同一套三栏或卡片模板。
- 不做卡片嵌套卡片。
- 不用假数据填满正式页面。
- 不暴露原始 JSON、内部模型名和英文技术状态。
- 不用乱码、无限加载、无操作空状态进入生产。
- 不把登录页的暖纸背景机械复制到每个内容容器。
